# Development profile configuration
spring.application.name=springboot_backend-dev

# Security configuration for development
app.security.enabled=false

# Development database - SQL Server Local with SQL Authentication
spring.datasource.url=jdbc:sqlserver://DESKTOP-V04UPUO:1433;database=lirium;encrypt=false;trustServerCertificate=false;
spring.datasource.username=admin1234
spring.datasource.password=admin123
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA / Hibernate for development - SQL Server
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# Development logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.example.springboot_backend=DEBUG

# CORS more permissive for development
cors.allowed-origins=*
cors.allowed-methods=*
cors.allowed-headers=*