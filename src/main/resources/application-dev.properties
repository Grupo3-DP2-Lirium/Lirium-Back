# Development profile configuration
spring.application.name=springboot_backend-dev

# Security configuration for development
app.security.enabled=false

# Development database (same as default for now)
spring.datasource.url=jdbc:postgresql://localhost:5432/lirium_db
spring.datasource.username=postgres
spring.datasource.password=932709296

# JPA / Hibernate for development
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Development logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.example.springboot_backend=DEBUG

# CORS more permissive for development
cors.allowed-origins=*
cors.allowed-methods=*
cors.allowed-headers=*